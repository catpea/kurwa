#!/usr/bin/env node

// import acl from './src/lib/acl/index.js';
//
// const user = {groups: 'administrator'};
// // const user = {groups: 'registered, administrator, programmer'};
// const verdict = acl.allows(user, 'view:diagnostics', 'hack:db');
// // const verdict = acl.allows(user, 'view:diagnostics', 'delete:user', '*:db');
// if(verdict.deny) console.log( acl.flash(verdict) );
